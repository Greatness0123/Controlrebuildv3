/**
 * Desktop installer URLs — leave empty until builds are hosted.
 * Documented in LANDING_DUMMY_AND_SOCIAL_LINKS.md at repo root (control_web/).
 */
export const DESKTOP_DOWNLOAD_URLS = {
  mac: '',
  windows: '',
  linux: '',
} as const;
